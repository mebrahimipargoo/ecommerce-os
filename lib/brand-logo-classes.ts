/**
 * Strict bounding box for tenant logos in HTML UI (Settings preview, AppShell, TopHeader).
 * High-resolution uploads scale down; aspect ratio preserved via object-contain.
 */
export const BRAND_LOGO_IMG_CLASSNAME =
  "h-auto w-auto max-h-[45px] max-w-[160px] object-contain shrink-0";
